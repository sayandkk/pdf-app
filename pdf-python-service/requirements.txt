# PDF → DOCX conversion
pdf2docx==0.5.8

# Web framework
fastapi==0.111.1

# ASGI server
uvicorn[standard]==0.30.1

# Multipart form support (required by FastAPI for file uploads)
python-multipart==0.0.9

# Optional but useful for extra type hints in older Pythons
typing-extensions>=4.0.0
